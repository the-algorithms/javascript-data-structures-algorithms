/*
def power(base, expt):
    # assume expt is non-negative integer
    if (expt == 0):
        return 1
    elif (expt % 2 == 0):
        return power(base, expt//2)**2
    else:
        return base * power(base, expt//2)**2

print(power(2,5)) # 32
*/
