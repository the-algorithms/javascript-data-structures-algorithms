var art = 'Artist'

// Return the Unicode of the first character in a string (the Unicode value for "H"):
console.log(art.charCodeAt(0))
// Convert a Unicode number into a character:
console.log(String.fromCharCode(97 + 3))