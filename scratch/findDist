const findDist = (pt1, pt2) => {
  // Math.sqrt(Math.pow(x0 - y0, 2) + Math.pow(x1 - y1, 2));
  const [x1, y1] = pt1, [x2, y2] = pt2;
  return Math.sqrt(Math.pow(Math.abs(x1 - x2), 2) + Math.pow(Math.abs(y1 - y2), 2));
}